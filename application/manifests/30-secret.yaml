# Placeholder value only — this file is committed, so it must never hold a real
# credential. The Helm chart takes the real value from a gitignored values file,
# or skips rendering a Secret entirely via secrets.existingSecret.
apiVersion: v1
kind: Secret
metadata:
  name: podinfo-secret
  namespace: podinfo
  labels:
    app.kubernetes.io/name: podinfo
    app.kubernetes.io/version: "6.14.1"
    app.kubernetes.io/managed-by: kubectl
type: Opaque
stringData:
  API_KEY: "dev-only-not-a-real-key"
