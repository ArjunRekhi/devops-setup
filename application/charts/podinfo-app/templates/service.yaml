apiVersion: v1
kind: Service
metadata:
  name: {{ include "podinfo-app.name" . }}
  labels:
    {{- include "podinfo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "podinfo-app.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
