apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "podinfo-app.name" . }}-config
  labels:
    {{- include "podinfo-app.labels" . | nindent 4 }}
data:
  PODINFO_UI_MESSAGE: {{ .Values.config.ui.message | quote }}
  PODINFO_UI_COLOR: {{ .Values.config.ui.color | quote }}
